# backend development with javascript

Starting with mongoose for data modelling (enums, creating small schema models in single model as well so that it can be used as array in another schema model)

CORS(cross origin resource sharing) - It allows us to make cross origin requests means if frontend is hosted at localhost:3000 and making a request
to a backend which is hosted at localhost:5000 then the request will not happen successfully if cors is not configured.